'use client';
import ThemeToggle from '../ThemeToggle';
import styles from './Player.module.css';

const Player = () => {
  return (
    <div>
      <ThemeToggle />
    </div>
  );
};

export default Player;
